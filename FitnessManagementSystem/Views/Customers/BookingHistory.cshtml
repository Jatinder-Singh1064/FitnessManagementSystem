@model IEnumerable<FitnessManagementSystem.Models.Slots>

@{
    ViewBag.Title = "BookingHistory";
    Layout = "~/Views/Shared/CustomerLayout.cshtml";
}

<h2>Booking History</h2>

@Html.Display("message")

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CustomerId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.GymId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SlotTimeID)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CustomerId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.GymId)
            </td>
            <td>
                @Convert.ToString(string.Format("{0:dd/MM/yyyy}", item.Date))
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SlotTimeID)
            </td>
        </tr>
    }
</table>